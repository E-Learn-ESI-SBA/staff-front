export interface CourseCategory {
	icon: string;
	bgColor: string;
	year: string;
	cycle: string;
	total: number;
}

export enum EFileType {
	PDF = 'pdf',
	IMAGE = 'image',
	DOCUMENT = 'document',
	CODE = 'code',
}
