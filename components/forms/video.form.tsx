import {TVideoSchema} from "@/types/chapter/zod";

type Props = {
    defaultValues:TVideoSchema
    sectionId: string;
}
export default function VideoForm ({defaultValues,sectionId}:Props ) {
    return (
        <form>

        </form>
    )
}